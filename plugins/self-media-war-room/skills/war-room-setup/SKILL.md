---
name: war-room-setup
description: 检查和配置自媒体作战室。当用户要求安装、初始化、体检、排查某个工具为何无法调用，或确认学员电脑能否运行整套工作流时使用。不在未获授权时自动安装系统软件或登录平台账号。
---

# 作战室体检

运行插件根目录的环境检查：

```bash
python3 ../../scripts/doctor.py --format markdown
```

## 处理规则

- 先用学员能听懂的方式报告“基础创作、成片评分与对标拆解、可选增强”三类结果；只有排障时才展开组件名称。
- 缺少系统依赖时说明它会影响哪个环节，不把可选能力说成整个插件不可用。
- 只有用户明确要求安装时才执行安装命令。
- 平台登录、Cookie、账号授权必须由用户本人完成或明确提供；不得自行读取浏览器凭据。
- 安装或配置后重新运行体检，确认状态确实改变。
- 学员版优先给无代码操作；技术细节放在故障排查里。
- 视频解析默认走本地识别，不要求学员提供付费接口密钥。首次使用可能需要联网下载本地识别组件，应提前说明等待时间，但不要求学员选择模型。
